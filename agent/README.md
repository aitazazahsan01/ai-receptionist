# Voice agent worker

Empty on purpose — this is built in Phase 3 ([docs/04-IMPLEMENTATION-PLAN.md](../docs/04-IMPLEMENTATION-PLAN.md)),
once Twilio call handling (Phase 2) works. It will contain the LiveKit Agents
worker that runs VAD → STT → LLM → TTS for each call.
